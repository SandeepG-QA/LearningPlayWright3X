//Array literal preferred
let browsers = ["chrome", "firefox", "webkit"];

// Array constructor
let scores = new Array(3); //creats [empty with 3 elements] one value is length
let scroes2 = new Array (1, 2, 3);

//Array from () - it converts a sting in multiple characters.
let chars = Array.from("hello");
//("h","e","l","l","o")
console.log(chars);
