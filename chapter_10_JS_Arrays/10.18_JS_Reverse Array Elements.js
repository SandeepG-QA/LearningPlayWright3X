let num = [1, 2, 3, 4, 5];
num.reverse();
console.log(num.join(" ")); // Output: 5 4 3 2 1 //reverse() method reverses the order of the elements in an array. The first array element becomes the last and the last becomes the first.