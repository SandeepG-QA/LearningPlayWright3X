let browsers = ["chrome", "firefox", "webkit"];
console.log(browsers[1]); //output will be "firefox"
//for negative indexation, use the at function. 
console.log(browsers.at(-1)); //output will be "webkit". "at" is a inbuilt function.
console.log(browsers[-1]); //out put will be "undefined"
console.log(browsers.length);
let fruit = [];
console.log(fruit.length); //output will be zero as there is no value in fruit array.
