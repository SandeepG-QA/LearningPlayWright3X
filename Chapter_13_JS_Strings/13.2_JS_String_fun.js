let str = "Hello, World!";
console.log(str.length); // start with 1
console.log(str[0]); // start with 0 in case of index
console.log(str[7]);
console.log(str.at(-1)); // it will count from end.

//CharAt() -  it will work same as str[0]

str.charAt(5);
console.log(str.charAt(5));
str.charCodeAt(0); //72 - ASCII code
console.log(str.charCodeAt(0));