// in hositing , var will automatically declare in case it is not declare before use.
console.log(a);// output - undefined
var a = "aman";
console.log(a); // changed - > output - aman

// Hoisting doesn't physically move you code. 
// it is a mental model to understand how the js engine handle declaration during compilation.