// ternary operator (Conditional operator) is the only operator that takes three operands. It is used to evaluate Boolean expressions. It is a shortcut for the if statement.   
//  condition ? value (if true) : value if false
let age = 20;
let isAdult = age >= 18 ? true : false;
console.log(isAdult); // true
