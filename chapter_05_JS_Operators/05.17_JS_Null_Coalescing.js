let amul = null;
let val = amul ?? "Nandani Milk";
console.log(val);

// it is basically used to save the code from break in case null value is provided.

let api_response = null;
let responsedata = api_response ?? "{}";
console.log(responsedata);

/*if the response comes then assign the same value , if the response doesn't come then will
assign my value*/

let api_response1 ="aman";
let responsedata1 = api_response1 ?? "{}";
console.log(responsedata1);